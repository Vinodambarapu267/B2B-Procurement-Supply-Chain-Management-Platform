name: Approval Service CI

on:
  push:
    branches: [main, develop]
    paths:
      - "approval-service/**"
      - "commons/**"

  pull_request:
    branches: [main, develop]
    paths:
      - "approval-service/**"
      - "commons/**"

jobs:
  build:
    uses: ./.github/workflows/reusable-service-ci.yml
    with:
      service-name: approval-service
      service-path: approval-service
      java-version: "21"
