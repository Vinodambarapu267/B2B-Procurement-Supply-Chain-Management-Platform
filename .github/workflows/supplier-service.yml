name: Supplier Service CI

on:
  push:
    branches: [main, develop]
    paths:
      - "supplier-service/**"
      - "commons/**"

  pull_request:
    branches: [main, develop]
    paths:
      - "supplier-service/**"
      - "commons/**"

jobs:
  build:
    uses: ./.github/workflows/reusable-service-ci.yml
    with:
      service-name: supplier-service
      service-path: supplier-service
      java-version: "21"
